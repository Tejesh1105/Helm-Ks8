apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: website-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: yourwebistename.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: web-project
            port:
              number: 80
